node_modules/
dist/
server/data/
*.db
*.db-wal
*.db-shm
*.bak
.env
.env.local
.env.production
.DS_Store

# Local deployment artifacts (may contain encryption keys, DB dumps, server paths)
deploy/

# Personal deployment scripts (contain keys/credentials — kept local)
deploy-pi.sh
update-hermes.sh

# Monetization planning — private, never committed
monetization/
